# Project Scaffold
## Description
Basic project scaffold for Node/Express applications.

## Getting Started
1. Clone the repository: `git clone https://github.com/your-repo/project-scaffold.git`
2. Install the dependencies: `npm install`
3. Start the server: `npm start`

## API Endpoints
* `GET /api/`: Welcome message